apiVersion: v1
kind: Service
metadata:
  name: flask-app-service
  namespace: jenkins
  labels:
    app: flask-app
spec:
  selector:
    app: flask-app
  ports:
    - name: http          # 👈 REQUIRED for Prometheus
      protocol: TCP
      port: 80
      targetPort: 5000
  type: ClusterIP
